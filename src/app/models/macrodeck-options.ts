export interface MacroDeckOptions {
  fingerprint: string,
  socketHost: string,
  socketPort: number,
  development: boolean,
  calibrationMode: boolean,
  buttonCount: number,
  maxButtonCurrent: number,
  server: boolean
}
