export interface MacroDeckOptions {
  fingerprint: string,
  socketHost: string,
  socketPort: number,
  development: boolean,
  calibrationMode: boolean,
  buttonCount: number,
  maxButtonAmp: number,
  server: boolean
}
